# REL-001 — Record the completion gate
**Date:** 2026-09-16 · **Commit:** this commit, deployed to Atlas as `94b9fd9` ·
**Environment:** local + network + atlas

Every line of `spec/ACCEPTANCE.md`, run against one commit. Two lines are
**partial**, and both are stated rather than smoothed over.

## Gate

| # | Requirement | Result |
|---|---|---|
| 1 | `scripts/spec-validate.sh` passes | **pass** — `task graph validation passed: 21 task(s)` |
| 2 | `go build`, `go vet`, `go test ./...` pass | **pass** — all clean on go1.25.0 |
| 3 | `/library` under 1s live, three runs | **pass** — 0.078 / 0.079 / 0.078 (was 5.34s) |
| 4 | `/library?page=2` under 1s live, three runs | **pass** — 0.082 / 0.082 / 0.083 (was 8.84s) |
| 5 | Every filter, sort and rendered field in `contracts/library-view.md` still works | **pass** — 13 URLs, all 200, all composing |
| 6 | `best_current_store` holds real storefront names after cutover | **pass** — 494 priced entries, 20 real storefronts, 0 category values |
| 7 | The UI renders those names, unmapped identifiers degrade to raw, never blank | **pass** — mapped and unmapped both render; `—` only for empty |
| 8 | Cheaper-on-GG.deals callout, link to the GG.deals page, no listing detail | **pass** — 284 live |
| 9 | `sqlc.yaml` and `queries/` gone, nothing references them | **pass with a note** — see below |
| 10 | `CLAUDE.md` and `README.md` route new sessions to `SESSION_SEED.md` | **pass** — 3 and 1 references |
| 11 | No API key or secret in the repository, evidence, or changelog | **pass** — `git grep` for the key returns nothing |
| 12 | Deployed instance healthy, no user data deleted | **pass with a note** — see below |

## Detail

**#1, #2**
```bash
$ bash scripts/spec-validate.sh
task graph validation passed: 21 task(s)

$ go build ./... && go vet ./... && go test ./...
?   	nisaba	[no test files]
?   	nisaba/db	[no test files]
ok  	nisaba/handlers	0.004s
ok  	nisaba/sync	0.004s
```

**#3, #4** — live, three consecutive runs each:

```
/library         0.078260  0.079144  0.077944
/library?page=2  0.082168  0.081976  0.083286
```

**#5** — every documented filter and sort, live:

```
/library                      200  0.078s  cards=200  pager=6
/library?page=2               200  0.082s  cards=200  pager=8
/library?page=21              200  0.111s  cards=33   pager=6
/library?sort=added           200  0.112s  cards=200  pager=6
/library?sort=playtime        200  0.075s  cards=200  pager=6
/library?sort=rating          200  0.078s  cards=200  pager=6
/library?q=bio                200  0.068s  cards=10   pager=0
/library?genre=Adventure      200  0.087s  cards=200  pager=6
/library?store=steam          200  0.087s  cards=200  pager=6
/library?steam_deck=playable  200  0.077s  cards=200  pager=5
/library?installed=1          200  0.060s  cards=0    pager=0
/library?favorites=1          200  0.059s  cards=0    pager=0
/library?platform=pc          200  0.077s  cards=200  pager=6
```

Pagination is intact: page 21 is the last of 21 and holds the remainder, 33
cards. `installed=1` and `favorites=1` render nothing because the database has
`is_installed=1` and `is_favorite=1` counts of **0** — data, not a broken filter,
confirmed by direct query. Fields on page 1: 200 cards, 195 proxied artwork
images, 72 Steam Deck badges, 31 multi-store-owned markers (the expression
`PERF-005` rewrote).

**#6, #7, #8** — `GG-004`, `GG-003`, `GG-006`. `best_current_store` before and
after, live:

```
before:  gg.deals/keyshop 282, gg.deals/retail 211, (null) 167, instant-gaming 7
after:   Steam 209, Fanatical 87, GOG 64, Humble Store 21, GameBillet 20,
         Playsum 18, Epic Game Store 18, GreenManGaming 13, Microsoft Store 8,
         GamesPlanet US 6, AllYouPlay 6, IndieGala Store 5, WinGameStore 4,
         PlayerLand 3, PlanetPlay 3, Ubisoft Store 2, Nuuvem 2, MacGameStore 2,
         GamersGate 2, EA Store 1
```

**#9 — pass, with the honest count.** Both paths are gone (`test ! -e sqlc.yaml`,
`test ! -d queries`). Thirteen files still contain the string `sqlc`, and the
gate line's "nothing references them" is true only in the sense that matters:
not one of them references the scaffolding as existing, and no code path could
load it.

```bash
$ git grep -I -l sqlc | wc -l
13
```

What the thirteen are:

| Files | Why the string is there |
|---|---|
| `spec/PRODUCT.md`, `spec/OPEN.md`, `spec/tasks.yaml`, `spec/ACCEPTANCE.md`, `spec/STATE.md`, `spec/contracts/database.md` | Record the 2026-08-01 ruling and name `BASE-002`/`BASE-003` |
| `spec/evidence/ASSESSMENT.md`, `BASE-002.md`, `BASE-003.md`, this file | Evidence for the deletion |
| `SESSION_SEED.md:95` | The tripwire: "about to add a query to `queries/*.sql` or run sqlc" → that scaffolding is deleted |
| `db/store.go:2` | Package header: "It is the only source of queries: the sqlc scaffolding was deleted" |
| `.changelog/UNRELEASED.md` | The one-line changelog entry for the removal |
| `schema/tech_stack.md` | A pre-implementation design doc, carrying an explicit "**Partly superseded (2026-09-16)**" banner naming `BASE-002`; its `sqlc` rows describe a plan that was never built, labelled as history rather than rewritten into a false present tense |

**#10**
```bash
$ grep -c SESSION_SEED CLAUDE.md README.md
CLAUDE.md:3
README.md:1
```

**#11** — `git grep` for the ITAD key across tracked files returns nothing, and
neither `spec/` nor `.changelog/` contains it. It exists in exactly two places:
the live `app_config` on Atlas, and the session transcript that carried it.

**#12 — pass, with the ruled exception.** The instance answers: dashboard 200,
wishlist 200, `/library` 200, `/mystery-packs` 303 → `/?login=1` (protected
route, unauthenticated request — correct), `/nonexistent-xyz` 404. User data is
intact: **667** wishlist entries and **4033** library games, unchanged from
before the deploy.

What *was* deleted is the pre-cutover price data — 18,335 `wishlist_price_history`
rows and 493 category-level `best_current_store` values — under Bobby's explicit
2026-09-16 ruling ("scorched earth for new data", "yes, we are starting with
clean data"). This gate line predates that ruling and reads as an absolute, so it
is recorded here as the one place the gate and the ruling differ, with the ruling
being the later and more specific instruction. No wishlist entry, no game, and no
user-authored field (priority, notes, target price, tags) was touched.

## Round status

**21 tasks: 21 `done`.** Both halves of the 2026-08-01 round are complete in
production — library responsiveness (5.63s → 0.081s against a <1s target) and
real storefront names in price data and the UI, with GG.deals repurposed as the
comparison source that drives the cheaper-elsewhere callout.
